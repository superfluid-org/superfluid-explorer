sources:
  - name: gda
    handler:
      graphql:
        endpoint: '{context.url:https://api.thegraph.com/subgraphs/name/superfluid-finance/protocol-feature-goerli}'
        retry: 5
documents:
  - './**/*.graphql'
codegen:
  namingConvention: change-case-all#pascalCase
  scalars:
    BigInt: string
    Bytes: string
    BigDecimal: string
  maybeValue: T | undefined